// 函数
JS 中的函数是正真的数值，想一般的数据类型。

// 函数直接量
函数直接量没有函数名，可出现在其他JS表达式中

// Function 构造函数
new Function("参数列表", "函数体");

// 对象
JS中的对象可以作为关联数组使用
访问对象的属性

object.pro
或
object['pro']

// 对象直接量(即对象初始化程序)
var point = {x:2, y:3};

null -> false -> 0 -> "null"

undefined 变量未声明，或者未赋值的变量
undefined != null
但是 == 运算符将其视为等同
使用 === 或 typeof 严格区分 undefined 和 null


Date
var date = new Date(2013, 4, 9);

RegExp
/^ABC[1-9][abcd]\b/


Error

